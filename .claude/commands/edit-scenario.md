Edit the game scenario in config/game.yaml.

Steps:
1. Read the current config/game.yaml
2. Present the current scenario configuration to the user (title, situation, players, goals)
3. Ask what changes to make (or apply changes from $ARGUMENTS if provided)
4. Update config/game.yaml with the changes
5. Validate the YAML is still valid
